# 26.2-dev → 26.2 stable migration plan

Last updated: 2026-06-23

Goal: migrate the exact-26.2 line from snapshot-era naming (`26.2-dev`) to stable `26.2` while preserving the `fabric-26.2-dev` module path.

- [x] Record current migration state and target files.
- [x] Update Gradle and `fabric.mod.json` metadata for stable `26.2`.
- [x] Switch 26.2 line to mixin-driven block glow (`BlockGlowGizmoMixin`) and remove direct post-tick renderer pokes.
- [x] Revalidate the 26.2 block-glow mixin point through MCDEV and retarget it from removed `LevelRenderer.extractLevel` to `LevelExtractor.extract`.
- [x] Normalize deploy script jar naming and stale-jar cleanup for stable `debugbridge-26.2-*`.
- [x] Update CI build/release artifact names and documentation references from `26.2-dev` to `26.2`.
- [x] Build stable `26.2` jar with JDK 25 and install it into the Prism `26.2` instance.
- [x] Run live bridge smoke validation after launching the Prism `26.2` instance.
- [x] Fix and rebuild lowercase entity equipment slot handling for `mc_get_entity_item_texture` on 26.2.
